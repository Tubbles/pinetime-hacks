# Log — 2026-08-04

## [init] 2026-08-04 — log split by date; progressive disclosure written into the methodology

- The monolithic `doc/LOG.md` (36 entries over 4 dates) is split into per-date files under `doc/log/`, with `doc/LOG.md` remaining as the index (one line per entry) so existing cross-references keep resolving. Rationale (user): gentler on an LLM's context window — load the index, open only the day files that matter.
- Progressive disclosure is now documented in `CLAUDE.md` as a central documentation methodology: focused `.md` files over monoliths, index files that link with one-line hooks, per-date logs, and `CLAUDE.md` files in subdirectories carrying subtree-specific context. No subtree `CLAUDE.md` files exist yet; add them as the subtrees accumulate enough local knowledge to warrant one.
